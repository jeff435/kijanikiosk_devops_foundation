pipeline {
    agent any

    stages {
        stage('Terraform: Staging Namespace') {
            steps {
                dir('capstone/terraform') {
                    sh 'terraform init'
                    sh 'terraform apply -auto-approve'
                }
            }
        }
        stage('Ansible: Staging Config') {
            steps {
                dir('capstone/ansible') {
                    sh 'ansible-playbook configure-staging.yml'
                }
            }
        }
        stage('Deploy to Staging') {
            steps {
                sh 'kubectl apply -f capstone/k8s/ -n kijani-staging'
            }
        }
        stage('Smoke Test') {
            steps {
                script {
                    sh 'sleep 10'
                    def health = sh(script: 'kubectl exec -n kijani-staging deployment/kk-payments -- curl -s http://localhost:3000/health 2>/dev/null || echo "unhealthy"', returnStdout: true).trim()
                    if (health != "healthy") {
                        error "Smoke test failed"
                    }
                    echo "Smoke test passed"
                }
            }
        }
        stage('Approval Gate') {
            input {
                message "Deploy to production?"
                submitter "admin"
                parameters {
                    string(name: 'REASON', defaultValue: '', description: 'Why approve this deployment?')
                }
            }
        }
        stage('Deploy to Production') {
            steps {
                sh 'kubectl apply -f capstone/k8s/ -n default'
            }
        }
    }
}
